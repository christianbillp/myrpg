export const TILE_SIZE = 50;
export const GRID_COLS = 26;
export const GRID_ROWS = 15;
export const HUD_HEIGHT = 130;
export const PLAYER_PANEL_WIDTH = 200;
export const TARGET_PANEL_WIDTH = 200;

/** Default token-related blue. Used as:
 *  - Fallback fill colour when an SVG token texture is missing
 *  - Nameplate text colour above NPC tokens (so creature names read in a
 *    unified accent rather than each def's individual hue). */
export const DEFAULT_TOKEN_COLOR = 0x3388ff;
export const DEFAULT_TOKEN_COLOR_HEX = '#3388ff';
